# Semantic Indices и Knowledge Graphs

Эта директория содержит семантические индексы и графы знаний для системы AndlancerOS.

## Структура

```
semantic_indices/
├── README.md                          # Этот файл
├── directory_index_services.json      # Индекс всех модулей и компонентов
└── knowledge_graph/                   # Детальные графы знаний по модулям
    ├── core_module.json               # Граф знаний для core модуля
    ├── apps_module.json               # Граф знаний для apps модуля
    ├── components_module.json         # Граф знаний для components модуля
    └── main_module.json               # Граф знаний для main модуля
```

## Назначение

### directory_index_services.json
**Быстрый индекс всех модулей** для:
- Поиска компонентов по ответственности
- Понимания структуры проекта
- Навигации по зависимостям

### knowledge_graph/*.json
**Детальная информация о модулях** включает:
- Контракты компонентов (что гарантирует, что требует)
- Точки интеграции (кто с кем взаимодействует)
- Архитектурные паттерны
- Технический долг и проблемы кода

### SAL-аннотации в коде
**Формализация контрактов в исходном коде** через теги:
- `@requires` - предусловия
- `@ensures` - постусловия
- `@invariant` - инварианты
- `@why_*` - бизнес-обоснование

## Стратегия обновления

### ✅ Обновлять ОБЯЗАТЕЛЬНО при:

1. **Архитектурных изменениях:**
   - Добавлен новый модуль/сервис
   - Изменена структура зависимостей
   - Новый компонент с публичным API

2. **Изменении контрактов:**
   - Изменились входные/выходные параметры методов
   - Добавлены/удалены зависимости
   - Изменены гарантии компонента

3. **Изменении точек интеграции:**
   - Компонент теперь используется другими модулями
   - Добавлены новые события в EventBus
   - Изменились паттерны взаимодействия

### ⚠️ Можно НЕ обновлять сразу при:

1. **Рефакторинге без изменения API:**
   - Улучшение читаемости кода
   - Оптимизация внутренней логики
   - Исправление багов без изменения интерфейса

2. **Мелких изменениях:**
   - Исправление опечаток в комментариях
   - Изменение стилей/форматирования

### 📋 Рекомендации

1. **Обновлять при значимых изменениях** - не каждый коммит, а когда меняется архитектура
2. **Периодическая проверка** - раз в спринт/релиз проверять актуальность
3. **Автоматизация** - можно создать скрипты для проверки соответствия структуры

## Использование

### Для разработчиков
1. Перед добавлением новой функции - проверьте `directory_index_services.json` чтобы не дублировать существующий функционал
2. При изменении компонента - обновите соответствующий knowledge graph
3. При добавлении зависимостей - обновите раздел `integration_points`

### Для ИИ агентов
1. Начинайте с `directory_index_services.json` для понимания структуры
2. Читайте соответствующие `knowledge_graph/*.json` для детальной информации
3. Изучайте SAL-аннотации в коде для понимания контрактов

## Примеры использования

### Поиск компонента
```json
// directory_index_services.json
"core_module": {
  "components": [
    {
      "name": "StorageManager",
      "file": "src/core/StorageManager.js",
      "responsibility": "Управление localStorage"
    }
  ]
}
```

### Понимание контрактов
```json
// knowledge_graph/core_module.json
"StorageManager": {
  "key_contracts": {
    "@requires": "localStorage доступен в браузере",
    "@ensures": "Данные сохраняются и загружаются корректно"
  }
}
```

### Чтение SAL-аннотаций
```javascript
// src/core/StorageManager.js
/**
 * @requires: localStorage доступен в браузере
 * @ensures: Данные сохраняются в localStorage с lastModified timestamp
 * @why_ensures: lastModified критичен для отслеживания актуальности данных
 */
save(data) { ... }
```

## Поддержка актуальности

- **Обновлять**: При архитектурных изменениях, изменении контрактов
- **Не обновлять**: При рефакторинге без изменения API, мелких изменениях
- **Проверять**: Раз в спринт/релиз на актуальность

## Автоматизация (TODO)

В будущем можно добавить:
- Скрипт проверки соответствия структуры файлов индексу
- Предупреждения если файл удален, но остался в индексе
- Автоматическое обновление при изменениях через git hooks
