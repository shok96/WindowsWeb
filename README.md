# AndlancerOS

Полнофункциональная веб-ОС, работающая в браузере с виртуальной файловой системой, оконным менеджером и множеством встроенных приложений.

![AndlancerOS](https://i.imgur.com/M87n1en.jpeg)

## 🚀 Возможности

- 🖥️ **Полноценный рабочий стол** с обоями и иконками
- 📁 **Виртуальная файловая система** с сохранением в localStorage
- 🪟 **Система многооконности** с поддержкой drag & drop и resize
- 🎨 **Смена тем** (светлая/тёмная)
- 🎭 **Персонализация** (обои, акцентный цвет)
- 💾 **Сохранение данных** между сессиями
- ⌨️ **Горячие клавиши**
- 📱 **Встроенные приложения**

## 📦 Встроенные приложения

1. **📁 Проводник** - Управление файлами и папками
2. **📝 Блокнот** - Текстовый редактор
3. **🔢 Калькулятор** - Выполнение вычислений
4. **📅 Календарь** - Просмотр календаря
5. **🌐 Браузер** - Веб-браузер с iframe
6. **💣 Сапёр** - Классическая игра
7. **💻 Терминал** - Командная строка
8. **📊 Диспетчер задач** - Управление процессами
9. **⚙️ Параметры** - Настройки системы

## 🛠️ Технологии

- **HTML5, CSS3, JavaScript (ES6+)**
- **Gravity UI** - UI компоненты и стили
- **Interact.js** - Drag & drop и resize
- **Vite** - Сборщик и dev-сервер
- **SCSS** - Препроцессор CSS
- **LocalStorage API** - Хранение данных

## 📋 Требования

- Node.js 16+ 
- npm или yarn
- Современный браузер (Chrome, Firefox, Edge, Safari)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repo-url>
cd AndlancerOS
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск dev-сервера

```bash
npm run dev
```

Приложение откроется автоматически по адресу `http://localhost:3000`

### 4. Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### 5. Предпросмотр продакшен сборки

```bash
npm run preview
```

## 🚢 Развертывание на CapRover

Чтобы развернуть проект на сервере CapRover, необходимо создать специальный архив `deploy.tar.gz`.

### 1. Сборка проекта

Убедитесь, что вы собрали последнюю версию проекта:

```bash
npm run build
```

### 2. Создание файла captain-definition

В корневой папке проекта создайте файл `captain-definition` (без расширения) со следующим содержимым:

```json
{
  "schemaVersion": 2,
  "dockerfileLines": [
    "FROM nginx:alpine",
    "COPY . /usr/share/nginx/html",
    "RUN rm /usr/share/nginx/html/captain-definition"
  ]
}
```

Этот файл сообщает CapRover, что это статическое веб-приложение, и как его обслуживать с помощью Nginx.

### 3. Создание архива для развертывания

Выполните следующую команду в терминале, чтобы создать архив `deploy.tar.gz`, готовый к загрузке в CapRover:

```bash
tar -czf deploy.tar.gz captain-definition -C dist .
```

Эта команда упаковывает `captain-definition` и все содержимое папки `dist` в один архив.

Теперь вы можете загрузить полученный файл `deploy.tar.gz` через веб-интерфейс CapRover.

## 🎮 Использование

### Первый запуск

1. При первом запуске вы увидите экран входа
2. Нажмите кнопку "Войти" для входа в систему
3. Откроется рабочий стол AndlancerOS

### Основные действия

- **Открыть меню Пуск**: Клик по кнопке Windows или клавиша `Win`
- **Запустить приложение**: Клик по иконке в меню Пуск или на панели задач
- **Создать файл/папку**: Правый клик на рабочем столе → Создать
- **Открыть файл**: Двойной клик по иконке файла
- **Переместить окно**: Перетащите за заголовок окна
- **Изменить размер окна**: Потяните за край окна
- **Развернуть окно**: Двойной клик по заголовку или кнопка развернуть
- **Закрыть окно**: Кнопка закрыть (X)

### Горячие клавиши

- `Win` - Открыть/закрыть меню Пуск
- `Ctrl + Alt + Delete` - Диспетчер задач
- `Win + E` - Проводник
- `Win + R` - Терминал

### Терминал

Доступные команды:
- `help` - Список команд
- `ls` / `dir` - Список файлов
- `cd <путь>` - Сменить директорию
- `mkdir <имя>` - Создать папку
- `pwd` - Текущий путь
- `clear` / `cls` - Очистить экран

### Персонализация

1. Откройте **Параметры** из меню Пуск
2. Выберите раздел **Персонализация**
3. Измените тему, акцентный цвет или обои
4. Изменения применяются мгновенно

### Экспорт/импорт данных

1. Откройте **Параметры** → **Система**
2. Нажмите **Экспорт данных** для создания резервной копии
3. Используйте **Импорт данных** для восстановления

## 📁 Структура проекта

```
WindowsWeb/
├── public/
│   ├── assets/          # Ресурсы (изображения, звуки, шрифты)
│   └── index.html       # Точка входа
├── src/
│   ├── apps/            # Приложения
│   │   ├── Calculator/
│   │   ├── Calendar/
│   │   ├── Notepad/
│   │   ├── Browser/
│   │   ├── Minesweeper/
│   │   ├── Terminal/
│   │   ├── TaskManager/
│   │   ├── Settings/
│   │   └── FileExplorer/
│   ├── components/      # UI компоненты
│   │   ├── LoginScreen/
│   │   ├── Desktop/
│   │   ├── Taskbar/
│   │   ├── StartMenu/
│   │   ├── Window/
│   │   └── ContextMenu/
│   ├── core/            # Ядро системы
│   │   ├── StorageManager.js
│   │   ├── FileSystem.js
│   │   ├── ProcessManager.js
│   │   ├── EventBus.js
│   │   ├── ThemeManager.js
│   │   └── Utils.js
│   ├── data/            # Данные по умолчанию
│   │   ├── defaultApps.js
│   │   └── systemCommands.js
│   ├── styles/          # Стили
│   │   ├── main.scss
│   │   ├── variables.scss
│   │   ├── animations.scss
│   │   └── components/
│   └── main.js          # Главный файл приложения
├── package.json
├── vite.config.js
└── README.md
```

## 🎨 Дизайн

Проект следует современной дизайн-системе:
- Rounded corners (8px border-radius)
- Backdrop blur эффекты
- Акриловые поверхности
- Мягкие тени
- Плавные анимации
- Центрированная панель задач
- Современное меню Пуск

## 🔧 Разработка

### Добавление нового приложения

1. Создайте папку в `src/apps/YourApp/`
2. Создайте класс приложения `YourApp.js`:

```javascript
export class YourApp {
  constructor(params = {}) {
    // Инициализация
  }

  render() {
    const container = document.createElement('div');
    container.className = 'yourapp-app';
    container.innerHTML = `<h1>Your App</h1>`;
    return container;
  }
}
```

3. Добавьте конфигурацию в `src/data/defaultApps.js`
4. Импортируйте и зарегистрируйте в `src/main.js`

### Стилизация

Используйте CSS переменные из `src/styles/variables.scss`:
- `--accent-color` - Акцентный цвет
- `--bg-primary` - Основной фон
- `--bg-secondary` - Вторичный фон
- `--text-primary` - Основной текст
- `--border-radius` - Скругление углов

## 🐛 Известные ограничения

- LocalStorage ограничен ~5-10 MB
- Некоторые сайты блокируют загрузку в iframe (Browser)
- Горячие клавиши могут конфликтовать с браузером
- Нет реальной сетевой функциональности

## 🚀 Будущие улучшения

- [ ] PWA поддержка (offline работа)
- [ ] Синхронизация между устройствами
- [ ] Больше приложений (Paint, Media Player)
- [ ] Виртуальная сеть между пользователями
- [ ] IndexedDB для больших файлов
- [ ] Анимации переходов между темами
- [ ] Поддержка виджетов

## 📝 Лицензия

MIT License - свободное использование в личных и коммерческих проектах

## 👨‍💻 Автор

Создано с использованием Cursor AI

## 🙏 Благодарности

- [Gravity UI](https://gravity-ui.com) - UI компоненты
- [Interact.js](https://interactjs.io) - Drag & drop библиотека
- [Vite](https://vitejs.dev) - Сборщик
- Microsoft - За вдохновение дизайном Windows 11

---

**Наслаждайтесь AndlancerOS! 🎉**

